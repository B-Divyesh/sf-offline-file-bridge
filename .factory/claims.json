[
  {
    "id": "offline-reload",
    "claim": "Works after the first visit, and ready demo files still open offline",
    "where": "landing facts, demo network state, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; visit /demo online, switch the context offline, reload, and open a ready sample file"
  },
  {
    "id": "demo-sandbox",
    "claim": "Demo sample data is isolated from real data",
    "where": "demo banner, privacy page",
    "test": "npm test -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context; assert demo-prefixed localStorage and no real IndexedDB database"
  },
  {
    "id": "demo-ready-sample",
    "claim": "Trying the sample opens a ready folder mirror with three realistic files",
    "where": "landing action note, demo",
    "test": "npm test -- --grep @claim:demo-ready-sample",
    "sandbox": "fresh browser context; click the landing action and assert the demo banner, Field notes, three sample files, ready state, and no real IndexedDB database"
  },
  {
    "id": "demo-reset",
    "claim": "Reset demo restores the displayed seeded sample state",
    "where": "demo banner",
    "test": "npm test -- --grep @claim:demo-reset",
    "sandbox": "fresh demo; refresh, reset without reload, and assert the seeded ready time and reset notice are visible"
  },
  {
    "id": "local-only",
    "claim": "Selected and demo files stay on the device",
    "where": "landing facts, privacy page",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "intercept demo refresh/open and browser-selected file import/open flows; assert every request is same-origin"
  },
  {
    "id": "freshness",
    "claim": "Each ready folder mirror shows its last successful refresh",
    "where": "landing preview, app folder record, terms",
    "test": "npm test -- --grep @claim:freshness",
    "sandbox": "demo data; refresh the folder and assert the visible ready time changes to just now"
  },
  {
    "id": "file-handoff",
    "claim": "A ready sample file can be opened and saved for another local app",
    "where": "demo file action, how it works",
    "test": "npm test -- --grep @claim:file-handoff",
    "sandbox": "demo data; open a ready Markdown file and assert its downloaded filename"
  },
  {
    "id": "scoped-folder-access",
    "claim": "Android uses the system folder picker without broad storage permission",
    "where": "landing how it works, privacy page",
    "test": "npm test -- --grep @claim:scoped-folder-access",
    "sandbox": "read the shipped Android manifest and native plugin; assert SAF use and absence of broad storage permissions"
  },
  {
    "id": "free-tier",
    "claim": "One folder mirror is free; Bridge Pro costs $14 once and keeps up to eight folder mirrors with 30 refresh records each",
    "where": "landing facts, pricing, terms",
    "test": "npm test -- --grep @claim:free-tier",
    "sandbox": "fresh landing page; assert the free limit, Pro capacity, exact price, and Sociobot checkout URL"
  },
  {
    "id": "browser-persistence",
    "claim": "In supported browsers, selected folder files stay available after reload",
    "where": "README browser/PWA feature list, app",
    "test": "npm test -- --grep @claim:browser-persistence",
    "sandbox": "fresh browser context; select two in-memory files through the folder input, reload /app, and assert both remain"
  },
  {
    "id": "native-refresh-safety",
    "claim": "A failed Android refresh keeps the previous ready local mirror",
    "where": "landing preview, app folder record, README",
    "test": "npm run test:unit -- -t @claim:native-refresh-safety",
    "sandbox": "native transaction regression test; a staged replacement is discarded before commit and the completed mirror remains intact"
  },
  {
    "id": "license-verification-privacy",
    "claim": "License verification sends a token only to the Sociobot billing API",
    "where": "privacy page",
    "test": "npm test -- --grep @claim:license-verification-privacy",
    "sandbox": "fresh landing page; enter a fixture token with the verify endpoint intercepted and assert its sole cross-origin request is the documented Sociobot endpoint"
  },
  {
    "id": "checkout",
    "claim": "Buy Bridge Pro opens the registered Sociobot hosted checkout",
    "where": "Bridge Pro purchase button",
    "test": "npm test -- --grep @claim:checkout",
    "sandbox": "request the advertised checkout URL without following redirects; assert a 303 to a hosted Dodo checkout session"
  },
  {
    "id": "consent-removal",
    "claim": "Removing an Android mirror deletes its local copy and releases its folder access",
    "where": "landing privacy section, privacy page, README",
    "test": "npm run test:unit -- -t @claim:consent-removal",
    "sandbox": "native plugin regression test; assert deletion, exact persisted SAF grant release, then preference removal"
  },
  {
    "id": "native-handoff",
    "claim": "Android opens ready private copies through the system app chooser",
    "where": "landing how it works, install walkthrough, README",
    "test": "npm run test:unit -- -t @claim:native-handoff",
    "sandbox": "native plugin/manifest regression test; assert a FileProvider URI, ACTION_VIEW chooser, and read grant"
  }
]
