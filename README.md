# Offline File Bridge

Live: https://offline-file-bridge.sociobot.in — built by the Param Factory (`android-apk`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
